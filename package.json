{
  "name": "joReact",
  "version": "0.0.1",
  "engines": {
    "node": "6.11.1",
    "npm": "3.10.10"
  },
  "scripts": {
    "start": "webpack -p --progress --config application/static/webpack.config.js",
    "heroku-postbuild": "cd application/static && npm install && npm run build"
  }
}
